<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.parkingbnb_proyecto">

    <!-- Permiso para consumir APIs REST -->
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.ParkingBnB_Proyecto">

        <!-- HOME (menú principal después del login) -->
        <activity
            android:name=".ui.HomeActivity"
            android:exported="false" />

        <!-- Registrar autos (SQLite) -->
        <activity
            android:name=".ui.AddAutoActivity"
            android:exported="false" />

        <!-- Listar autos desde API (Retrofit) -->
        <activity
            android:name=".ui.ListaAutosActivity"
            android:exported="false" />

        <!-- Listar autos agregados manualmente (SQLite) -->
        <activity
            android:name=".ui.ListaAutosLocalesActivity"
            android:exported="false" />

        <!-- Agregar Usuario (solo admin) -->
        <activity
            android:name=".ui.AgregarUsuarioActivity"
            android:exported="false" />

        <!-- LOGIN (pantalla inicial) -->
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
